---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: projectmembers.shpyrd.io
spec:
  group: shpyrd.io
  names:
    kind: ProjectMember
    listKind: ProjectMemberList
    plural: projectmembers
    shortNames:
    - member
    singular: projectmember
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.project
      name: Project
      type: string
    - jsonPath: .spec.role
      name: Role
      type: string
    - jsonPath: .spec.user
      name: User
      type: string
    - jsonPath: .spec.team
      name: Team
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProjectMember grants a role on a project to a user or a team.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProjectMemberSpec grants a project role to a user or a team.
            properties:
              project:
                description: Project name (the namespace is app-<project>).
                type: string
              role:
                description: Role is viewer, developer or admin.
                enum:
                - viewer
                - developer
                - admin
                type: string
              team:
                description: Team name.
                type: string
              user:
                description: User email; exactly one of User and Team is set.
                type: string
            required:
            - project
            - role
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
