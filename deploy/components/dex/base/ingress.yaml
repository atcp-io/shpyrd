apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dex-tls
spec:
  secretName: dex-tls
  dnsNames:
    - auth.${SHPYRD_DOMAIN}
  issuerRef:
    kind: ClusterIssuer
    name: ${SHPYRD_PLATFORM_ISSUER}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dex
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: ${SHPYRD_PLATFORM_INGRESS_CLASS}
  tls:
    - hosts:
        - auth.${SHPYRD_DOMAIN}
      secretName: dex-tls
  rules:
    - host: auth.${SHPYRD_DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dex
                port:
                  name: http
