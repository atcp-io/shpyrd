# OCI: snapshots are Block Volume backups, incremental after the first.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../components/snapshot-controller/base
  - snapshot-class.yaml
