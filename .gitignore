# binaries and tools
/bin
*.out

# editors / OS
/.idea
.vscode
*.swp
.DS_Store
.envrc
.tool-versions

# node
node_modules
coverage
*.lcov
.cache

# built dashboard (embedded into the server binary); keep the placeholder
ui/dist/*
!ui/dist/.gitkeep

# exported manifests
/shpyrd-export

# local certificates and state
*.pem
*.tfstate
*.tfstate.*
.terraform
dist/
hack/oci/.out.env

# terraform (contrib): the lock file is committed, local variables are not
terraform.tfvars
/shpyrd-server
*-dns.pem

# terraform (contrib/aws): the VPN profile is a credential
*-vpn.ovpn

# terraform (contrib): the vars files are regenerated by apply
*.vars
*-vpn.conf
